apiVersion: v1
kind: Service
metadata:
  name: {{ include "datapipeline-chart.fullname" . }}
  labels:
    {{- include "datapipeline-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datapipeline-chart.selectorLabels" . | nindent 4 }}
